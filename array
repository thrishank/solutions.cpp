#include <iostream>
using namespace std;

int main()
{
    // int a[10];
    // a[0] = 20;
    // a[9] = 10;

    // cout << a[9] << " " << a[0];
    // int a[4] = {1, 23, 45, 56};   // Intialisaation
    // cout << a[1];
    // printing array using for loop
    // int a[10] = {1, 2, 3, 4};
    // for (int i = 0; i <= 10; i++)
    // {
    //     cout << a[i];
    // }
    // Intialize an empty array .And take input from for loop & print it
    // int a[10];
    // for (int i = 0; i < 10; i++)
    // {
    //     a[i] = i * i;
    //     cout << a[i] << " ";
    // }
    // cout << endl;
    // sum of array elements
    int sum = 0;
    int a[10];
    for (int i = 0; i < 10; i++)
    {
        a[i] = i * i;
        sum = sum + a[i];
    }
    cout << sum;

    return 0;
}
// Array
// int a[n]; // can only store int no float no char
// Array is used to store multiple variables
// n blocks will form
// numbering in array start's from 0
// suppose int a[10] = {1,2,34,45,0} .In these array rest of the elements will become zero
// print the sum of array elements
// for (int i = 0; i < n; i++)
// {
//     cin >> a[i];
// }
// // printing output
// for (int i = 0; i < n; i++)
// {
//     cout << a[i] << " ";
// }
// cout << endl;
// // sum
// int sum = 0;

// for (int i = 0; i < n; i++)
// {
//     sum = sum + a[i];
// }
// cout << sum << endl;
// print largest number & smallest number in array
// int smallest = INT_MAX;
// int largest = INT_MIN;
// for (int i = 0; i < n; i++)
// {
//     if (a[i] > largest)
//     {
//         largest = a[i];
//     }
//     if (a[i] < smallest)
//     {
//         smallest = a[i];
//     }
// }
// cout << largest << endl;
// cout << smallest;
